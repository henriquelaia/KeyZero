PORT=3001
FRONTEND_URL=http://localhost:5173
RP_ID=localhost

# MongoDB (Atlas / Remote)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/?retryWrites=true&w=majority&appName=KeyZero
DB_NAME=keyzero

# JWT — gera com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_GENERATE_A_STRONG_RANDOM_SECRET
JWT_EXPIRES_IN=7d
